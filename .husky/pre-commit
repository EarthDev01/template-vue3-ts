set -e

# Policy: ห้าม commit ตรง branch ที่กำหนดไว้
node scripts/hooks/block-protected-branch.mjs

# ตรวจชื่อไฟล์ (staged)
node scripts/hooks/check-filenames.mjs

# Format + lint + sort import (เฉพาะไฟล์ staged)
npm exec lint-staged --verbose
